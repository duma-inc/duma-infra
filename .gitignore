# Variáveis de ambiente (contém segredos — use .env.example como referência)
.env

# Backups e snapshots locais do banco de dados
*.sql
backups/

# Backups gerados por update_host_ip.sh (contêm segredos)
.env.bak-*
